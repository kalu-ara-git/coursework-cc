apiVersion: v1
kind: Service
metadata:
  name: patient-record-service-uat
spec:
  selector:
    app: patient-record-service-uat
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
