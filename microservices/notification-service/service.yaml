apiVersion: v1
kind: Service
metadata:
  name: patient-notification-service
  namespace: default
spec:
  selector:
    app: patient-notification-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
